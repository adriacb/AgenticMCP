from mcp.server.tools.tool_manager import ToolManager

"""This class is used to manage the tools for the MCP agent.
    - get_tool
    - list_tools
    - add_tool(fn, name, description, annotations)
    - call_tool(name, arguments, context)
"""